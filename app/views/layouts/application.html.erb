<!DOCTYPE html>
<html>
  <head>
    <title>MembersOnly</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap-icons@1.4.0/font/bootstrap-icons.css" %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <% if member_signed_in? %>
      <p>You are logged in</p>
      <%= link_to current_member.name, edit_member_registration_path %>
      <%= link_to "Log out", destroy_member_session_path, method: :delete %>
    <% else %>
      <%= link_to "Sign In", new_member_session_path %>
      <%= link_to "Sign Up", new_member_registration_path %>
    <% end %>
    <%= yield %>
  </body>
</html>
